#!/usr/bin/env node

'use strict';

var Path = require( 'path' ),
	Fs = require( 'fs' ),
	bin = Path.join( Path.dirname( Fs.realpathSync( __filename ) ), '../bin' );

require( bin + '/ready-primes.js' ).ReadyPrimes;
