#!/usr/bin/env node

'use strict';

var Path = require('path'),
    Fs = require('fs'),
    bin = Path.join(Path.dirname(Fs.realpathSync(__filename)), './');

require(bin + '/ready-primes.js').ReadyPrimes;
